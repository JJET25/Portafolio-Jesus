# ——— Contact form (Resend) ———
RESEND_API_KEY=
CONTACT_EMAIL=jespinoza2511@hotmail.com
CONTACT_FROM="Portfolio Contact <onboarding@resend.dev>"

# ——— Admin / Updates ———
# Password required to access /admin and write to /api/updates
ADMIN_PASSWORD=

# ——— Persistent storage for Updates (optional) ———
# Provision a Vercel KV store; these variables are injected automatically.
# Without them, updates are stored in memory and reset on every deploy.
KV_REST_API_URL=
KV_REST_API_TOKEN=
